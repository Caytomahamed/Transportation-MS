const newBtn = document.querySelector(".addUserBtn");
const newBtn = document.querySelector(".addUserBtn");

addNewBtn &&
  addNewBtn.addEventListener("click", () => {
    openModel(document.querySelector(".add"));
});
